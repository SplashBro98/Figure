package edu.epam.figure.observer;

import java.util.EventObject;

public class TetraEvent extends EventObject {
    public TetraEvent(Object source) {
        super(source);
    }

    @Override
    public Object getSource() {
        return super.getSource();
    }
}
